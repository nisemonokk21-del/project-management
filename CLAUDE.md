# プロジェクトメモ（案件管理アプリ）

## ⚠️ バージョン更新は毎回マスト（必須ルール）

**コードに変更を加えたら、コミットする前に必ずバージョンを更新すること。例外なし。**

更新箇所は2つ、両方を必ず揃える:

1. `src/App.jsx` のフッター `<footer className="version-footer">vX.Y — 変更内容の要約</footer>`
   - 数字を上げ、`—` の後ろの説明をその変更内容に書き換える。
2. `package.json` の `"version"`（`X.Y.0` 形式で `App.jsx` の `vX.Y` と一致させる）。

バージョンの上げ方: 小さな変更はマイナー（v3.1 → v3.2）、大きな節目はメジャー（v3.x → v4.0）。

## ⚠️ PR作成も毎回マスト（必須ルール）

**作業ブランチに push したら、毎回必ずデフォルトブランチ宛の PR を作成すること。** ユーザーから毎回指示されなくても自動でやる。既に同じブランチの PR が開いている場合は、新規作成せずその PR を使う（push すれば自動更新される）。

- PR のベース（マージ先）はデフォルトブランチ `claude/actor-project-management-app-P1ljj`（デプロイ対象ブランチ）。
- PR タイトル・本文には今回のバージョン番号（例: v3.1）と変更概要を含める。

## ⚠️ マージまで毎回マスト（必須ルール）

**PR を作成したら、毎回そのままマージまで完了させること。** ユーザーから毎回指示されなくても自動でやる。
マージしないと本番サイトに反映されず、ユーザーが動作を確認できないため、PR を開いたまま終わらない。

- マージ前に `npm run lint` / `npm run build` / `npm test` が通っていることを確認する。
- マージ後はデプロイ（`deploy.yml`）の成否まで確認し、結果をユーザーに伝える。
- 例外: ユーザーが明示的に「マージしないで」と言った場合のみ、マージせず PR のまま残す。

## デプロイ

- GitHub Pages への自動デプロイは `.github/workflows/deploy.yml` が管理。
- デプロイが走るのは `branches:` に列挙されたブランチ（`claude/actor-project-management-app-P1ljj` など）への push / マージ時のみ。
- 作業用の feature ブランチに push しただけでは本番サイトに反映されない。反映にはデプロイ対象ブランチへのマージ（＝上記 PR のマージ）が必要。
- マージ後、サイト下部の表記が更新後のバージョンに変われば反映完了の目印。

## 確認コマンド

- Lint: `npm run lint`
- ビルド: `npm run build`
